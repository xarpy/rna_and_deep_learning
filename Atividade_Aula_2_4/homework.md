# Atividade Aula 2.4

Utilizando como base o código `2.4-LinearRegressionIrisPyTorch.ipynb`, faça as 3 atividades abaixo. Para submissão, submeta sua resposta no repositório criado para a disciplina.

> **Observação:** Coloque o resultado dentro de uma pasta chamada **"Atividade Aula 2.4"**.

---

### 1. Uso do `loss.data`
Por que no final da varredura do laço de treinamento, a perda é impressa utilizando `loss.data`? Por que precisa do `data`?

### 2. Cálculo da Perda Pós-Treinamento
Calcule e imprima o valor da perda (MSE) fazendo o `predict` da rede com os pesos já treinados.

### 3. Histórico dos Parâmetros
Crie duas listas `w0_list` e `w1_list` para guardar a evolução dos valores dos parâmetros durante o laço de treinamento.